"""
接口层 / 表现层 (Interfaces / Presentation Layer)

DDD 架构中与用户直接交互的入口层，负责接收用户输入、组装依赖、驱动应用层执行。
本层是系统的"胶水代码"，通过依赖注入将基础设施层的具体实现绑定到应用层的抽象端口。

核心职责：
- 提供主程序入口 (main_cli)：命令行交互界面 (CLI)。
- 依赖注入组装：在启动时将 LLM 适配器、工具集、追踪器等实例注入到工作流中。
"""
